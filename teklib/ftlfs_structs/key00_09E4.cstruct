// Saved setup slot 0, firmware 3.15 observed length.
// Older command/PARM setup serializer image.  It is shorter than the current
// descriptor-id-1 image (0x9E0 vs 0xA30 payload) and should be indexed from a
// matching v3.15 listing before applying the current 0xA30 offset map.

typedef struct
{
    uint32 statusMarker; // observed v3.15 marker: 0xEE
    uint8 legacyCommandParmSetupImage[2528];
} file;
